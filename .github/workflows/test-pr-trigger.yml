name: "Test PR Trigger"

on:
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Echo test
        run: echo "PR trigger works! PR#${{ github.event.pull_request.number }}"
